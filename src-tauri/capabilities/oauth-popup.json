{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "oauth-popup",
  "description": "Capabilities for OAuth popup window (dev mode)",
  "windows": ["oauth-popup"],
  "permissions": ["core:window:allow-close", "core:event:allow-emit-to"]
}
